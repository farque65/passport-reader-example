import { CeramicGenesis, Tulons } from "tulons";
import { Passport } from "../../types/src";
export declare type CeramicStreams = Record<string, string | false>;
export declare type CeramicStamp = {
    provider: string;
    credential: string;
};
export declare type CeramicPassport = {
    issuanceDate: string;
    expiryDate: string;
    stamps: CeramicStamp[];
};
export declare class PassportReader {
    _tulons: Tulons;
    _ceramic_gitcoin_passport_stream_id: string;
    constructor(url?: string, network?: string);
    getGenesis(address: string): Promise<CeramicGenesis | false>;
    getPassport(address: string, streams?: CeramicStreams): Promise<CeramicPassport | Passport | false>;
    getPassportStream(address: string, streams?: CeramicStreams): Promise<CeramicPassport | false>;
}
